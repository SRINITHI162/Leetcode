class Solution {
    public int reverse(int x) {
        int digit=0;
        
        while(x>0)
        {
            int num=x;
            digit=digit+x%10;
            num=num/10;
            

        }
    return digit;
    }
}
